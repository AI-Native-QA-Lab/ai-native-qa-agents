# v0.1 Test Quality Dataset

Fixture layout follows the v0.1 eval contract. `good`, `weak`, `fake`, and `ambiguous` cases are grouped by language and framework; the dependency-free catalog in `packages/qa_agent/evals.py` runs the 155 labeled regression variants.
