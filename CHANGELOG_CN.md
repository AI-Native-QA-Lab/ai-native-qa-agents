# 更新日志

## 0.3.1 - 2026-09-08

### 修复

- 恢复已批准方案中的默认本地临时目录 pytest 沙箱；Docker 隔离改为可选（`--execution-backend docker`）。
- 优先用当前解释器定位 pytest，再回退到 `PATH`，避免 Homebrew 多 Python 布局下 CLI 评测失败。
- 补齐 v0.3 循环：显式失败分析、修复尝试，以及执行后的确定性 Reviewer 门禁。
- 同步根 README 与 v0.3 工程包，使其与「本地优先」执行契约一致。

### 测试

- 覆盖本地沙箱隔离、可选 Docker 不可用，以及 `analyze_failure` 修复轨迹。

## 0.3.0 - 2026-09-08

### 新功能

- 新增有界测试工程契约、测试生成、修复尝试、结构化质量门禁与 CLI 输出。
- 新增隔离的 Docker pytest 执行后端：源代码只读挂载、禁用网络、移除 Linux capabilities，并限制资源。
- 完成 v0.2 需求分析收尾：补充可追溯上下文、确定性的冲突处理与有界证据收集。

### 测试

- 扩展 v0.2 与 v0.3 的评估覆盖，涵盖接受、拒绝和不安全补丁路径。

### 文档

- 新增 v0.2、v0.3 中文工程入口文档，以及已批准的 v0.2 收尾与 v0.3 执行计划。

## 0.2.0 - 2026-09-07

### 新功能

- 新增 Requirement Intelligence 基线。
